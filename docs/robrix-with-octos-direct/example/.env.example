# Copy this file to `.env` in the same folder, then fill in your real key.
# `.env` holds secrets — never commit it.
#
#   cp .env.example .env
#
# The value below must match `api_key_env` in myagent.json (default: DEEPSEEK_API_KEY).
DEEPSEEK_API_KEY=sk-replace-with-your-deepseek-key
